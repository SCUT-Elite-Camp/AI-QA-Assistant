# External evaluation datasets

Large third-party corpora are stored here locally and are intentionally not
committed. For FinanceBench the expected location is:

```text
eval/datasets/external/financebench/
```

Keep derived, small, auditable Agent evaluation manifests under
`eval/datasets/` so they can be reviewed without committing source PDFs.
