# Mock 模式开关（默认使用 Mock，设置为 false 时请求真实 Agent）
VITE_USE_MOCK=true

# 真实 AI Gateway 配置（VITE_USE_MOCK=false 时需要）
AI_GATEWAY_API_KEY=

# GitHub OAuth 认证（可选）
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=
SESSION_SECRET=

# 数据库配置（默认使用本地 SQLite，生产环境使用 Turso）
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
